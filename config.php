<?php
/**
 * Application configuration loader.
 *
 * This is a thin wrapper that pulls in the single source of truth at
 * php_scripts/config.php. Edit credentials there (or via environment
 * variables) — never maintain two copies.
 */
require_once __DIR__ . '/php_scripts/config.php';
